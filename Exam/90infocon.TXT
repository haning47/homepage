
  台 北市九十學年度高級中學
資訊學科能力 競賽    筆試試卷
說明
I作答時間60分鐘。若須計算或作圖，請利用本試卷的空自處。
2．本筆試試卷題目均為填充題，共25格，每格4分。滿分為100分
3．答案必須按題號依序墳人「答案卷」上之空格內，否則不予計分

1.當你的作業系統跑了許多應用程式之後，你的電腦開始把大量的時間花在硬碟
  讀取上，這是因為 -- 不足所致。
2．燒錄在ROM上的基本輸入輸出系統其英文之簡稱為---.
3．假設有一計算機有32位元組記憶體，若第一個記憶體的位址為0000，則最後
  一個記憶體的位址為-- .                 。
4屬於網路通訊中OSI實體層的網路設備，將區域網路中的乙太網路連結成星狀
  網路，以降低網路無法接通的機率的設備稱為--.                  。
5．針對世界資訊的流通，希望將諸如繁體字的Big5碼、簡體字的GB碼、日文、
  韓文、希臘文、阿扯伯文等字碼統一的納入、編碼標準為 ---  。
6．有一個佇列(Queue)初始時為空的，按著執行以下動作:
  AddQA;AddQB;AddQC;DeleteQ;AddQD;AddQE;DeleteQ
  請問最後一個DeleteQ會取出何值?                         。       
7．有一個堆疊Stack初始時為空的，按著執行以下動作:
  PushA;PushB;PushC;Pop;PushD;PushE;Pop
  請問最後一個Pop會取出何值?      
                            。
8．如果要設計一個計算1+2+3+n 總和的遞迴程式,下列的程式碼之填空處應
  墳人何項?
     function sum(n)
      begin
          if n=1 then sum:=1
          else sum:=---
      end.

9．在電腦教室中，30台個人電腦共享一台由印表伺服器管理的雷射印表機。在印
表伺服器中使用何種資料結構來管理印表工作? 
10．在一個二元樹中，任意兩個節點可以找到__  條路徑。
11．在一個樹中，如果有 N 個節點 (node)，則有 --  個邊(edge)。  
12．請問在執行以下程式片段後，A[6]元素內的值為何? ---                  
     a[1]:= 1;
     a[2]:==1; 
     for i =3 to 10 do 
          a[i]:=a[i-1] +a[i-2]

13．現代的電腦作業系統使用 --    技術而使得有限大小的主記憶可以執行和儲存比它
容量大的程式和資料。
14．近年來許多人開始使用PDA來處理自己的資料，計寫出PDA的英文全名---.
15．有些個人電腦會使用  Celeron950 處理器，其 950 代表的單位是 ----.
16．奈秒(nanosecond)是計量電腦速度的微小時間單位之一．，一奈秒等
於 ---- 秒。 
17．數據機之功能是於資料傳輸過程中作數位與  -----  信號的轉換。 
18．「640K記憶體」這個名詞所指的是  ----- bits。  
19．若一彩色螢幕的最大解析度為 1024X768 像點，每儲存一個像點需要3 byte 的
記憶體空間，則若要將整個螢幕的內容儲存下來需至少 --- Mbytes的記
憶體空間。(請計算到小數第二位).
